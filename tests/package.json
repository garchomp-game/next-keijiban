{
  "name": "tests",
  "private": true,
  "scripts": {
    "e2e": "playwright test",
    "smoke": "artillery run ws-smoke.yml",
    "lint": "echo 'no lint'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "artillery": "^2.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
